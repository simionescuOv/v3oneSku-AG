# Copiază acest fișier în `.env` (sau `.env.local`) și completează cu valorile
# reale din Settings → API ale proiectului tău Supabase. NU comita `.env`.

VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
